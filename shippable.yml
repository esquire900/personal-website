language: node_js

install:
  - npm install -g bower gulp
  - npm install
  - bower install
  - sudo apt-get install -y sshpass

script:
  - gulp build

after_script:

after_success:
  - mv dist ../
  - rm -rf *
  - mv ../dist/* ./
  - sshpass -p "352s7wevz9f" git push -f root@192.121.166.203:../var/repo/site.git master

notifications:
  email:
    recipients:
      - simonnouwens@gmail.com
    on_success: never
    on_failure: always